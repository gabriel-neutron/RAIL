// Phase 3: subscribes to the `audio-chunk` binary Tauri event and pushes PCM
// samples into a Web Audio AudioContext. See docs/ARCHITECTURE.md §5.

export const useAudio = (): void => {
  // intentionally empty stub — implemented in Phase 3
};
