// Phase 1: subscribes to the `waterfall-frame` binary Tauri event and forwards
// Float32Array frames to the canvas renderer. See docs/ARCHITECTURE.md §3.

export const useWaterfall = (): void => {
  // intentionally empty stub — implemented in Phase 1
};
